development:
  adapter: async

test:
  adapter: test

benchmark:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: WordleAPI_production
